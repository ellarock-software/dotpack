## Summary

<!-- What does this PR change, and why? -->

## Related issues

<!-- e.g. Closes #123 -->

## Checklist

- [ ] `gofmt`, `go vet ./...`, `go test ./...`, and `go build ./cmd/dotpack` pass
- [ ] `golangci-lint run ./...` passes
- [ ] `mkdocs build --strict` passes (if docs changed)
- [ ] `gitleaks git . --config .gitleaks.toml --redact --no-banner` is clean
- [ ] Updated README, CLI help, and schema docs for any behavior change
- [ ] Added or updated tests for behavior changes
- [ ] No secrets, private paths, or organization-specific examples added

<!-- Adding a new host adapter? See "Adding a New Host Adapter" in CONTRIBUTING.md. -->
